import React from 'react';
import { FounderCard as BaseFounderCard } from '../home/FounderCard';

export const FounderCard = (props) => {
  return <BaseFounderCard {...props} />;
};

export default FounderCard;
